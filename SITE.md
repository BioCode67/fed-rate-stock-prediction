# QUANT LAB — 나스닥 퀀트 모의투자 사이트

**https://biocode67.github.io/fed-rate-stock-prediction/**

실제 나스닥100 데이터로 퀀트 기법과 AI 모델을 시험하고, 대회 방식으로 겨루는 교육용 터미널입니다.
학생은 **아무것도 설치하지 않고, API 키도 없이** 브라우저만 열면 됩니다.


## 화면 구성

| 탭 | 하는 일 | 배우는 것 |
|---|---|---|
| 0. 시작하기 | 14단계 커리큘럼 · 지금 할 일 한 가지 · 오늘의 시장 | 무엇부터 해야 하는가 |
| 1. 마켓 | 나스닥100 전 종목 정렬·검색, 섹터별 성과, 종목별 차트, 지수·매크로 | 종목·섹터 간 성과 차이가 얼마나 큰가 |
| 2. 장중 | 30분봉, 시간대별 변동성, 밤사이 갭 | 백테스트의 "종가 체결" 가정이 어디까지 현실적인가 |
| 3. 팩터 분석 | 팩터 9개의 IC·ICIR·t값·분위 수익·상관행렬 | 전략을 만들기 전에 신호가 진짜인지 확인하는 법 |
| 4. 알파 만들기 | 알파 식 언어 + BRAIN식 롱숏 시뮬레이터 · Sharpe/Fitness/Turnover 채점 | IQC에 제출하는 '식 한 줄'을 직접 쓰는 법 |
| 5. 전략 실험실 | 퀀트 기법 11종 + AI 4종 + 내가 만든 알파 백테스트, 비용 민감도, 롤링 성과 | 같은 조건에서 공정하게 비교하는 법 |
| 6. 모의투자 | 과거 시점부터 하루씩 진행. 직접 매매 또는 전략 자동매매 | 낙폭을 실제로 견디는 경험 |
| 7. 순위표 | 채점 구간 성과 리더보드 · 줄을 눌러 설정·고른 종목 열람 · 원클릭 재현 | 재현 가능한 결과만이 신뢰받는다 |
| 8. 연구 노트 | 시도 자동 기록, 최고기록 곡선, 메모, 마크다운 내보내기 | 시도 횟수를 밝히는 일이 왜 힘이 되는가 |
| 9. 배우기 | 퀀트의 흐름, 흔한 함정, 실습 과제, IQC 로드맵, 대회 준비, 용어집 | 심사위원이 보는 기준 |


## 대회 방식 (이 사이트의 핵심)

```
[--------- 개발 구간 ---------][--- 채점 구간(잠김) ---]
 여기서 마음껏 실험              제출해야 열림
```

백테스트는 **개발 구간에서만** 돌아갑니다. 제출 버튼을 누르면 같은 설정으로 채점 구간을 다시 돌려
그 성적이 순위표에 오릅니다. 캐글의 Public/Private 리더보드, WorldQuant IQC의 아웃오브샘플 평가와
같은 구조입니다.

제출 직후 개발 구간과 채점 구간 성과를 나란히 보여 주고, 차이가 크면 과적합 가능성을 경고합니다.
**개발 구간에서 좋았던 전략이 채점 구간에서 무너지는 것을 직접 보는 것**이 이 사이트에서 가장 중요한 경험입니다.

순위표는 성과만 받지 않습니다. 전략 설정과 리밸런싱마다 고른 종목을 함께 저장하고,
**순위표에서 줄을 누르면 그 자료를 열어 볼 수 있습니다.** 고전 전략이라면 `같은 설정으로
내 실험실에서 돌려 보기` 버튼으로 설정을 그대로 옮겨 담아 직접 재현할 수 있습니다.
"재현 가능하다"는 말이 화면에서 실제로 성립하게 만든 부분입니다.


## 부정직한 결과를 막는 장치들

이 사이트는 좋은 숫자를 만들어 주는 도구가 아니라, **좋아 보이는 숫자를 의심하는 훈련 도구**입니다.

| 장치 | 막는 것 |
|---|---|
| 리밸런싱 날 종가까지의 정보만 사용, 수익은 다음 날부터 반영 | 미래 정보 사용(look-ahead) |
| 개발/채점 구간 분리, 제출 후에만 채점 구간 공개 | 과적합 |
| 백테스트 실행 8회 초과 시 다중검정 경고 | 여러 번 돌려 좋은 것만 고르기 |
| 회전율만큼 비용 차감 + 손익분기 비용 표시 | 거래비용 무시 |
| 모든 성과를 QQQ 매수후보유와 나란히 표시 | 벤치마크 없는 자랑 |
| 샤프·MDD·회전율·t값 동시 표시 | 수익률만 보기 |
| 순위표 최소 60거래일 제한 | 짧은 표본 |
| 생존 편향을 화면과 문서에 명시 | 상장폐지 종목 누락 |
| 섹터 집중도 40% 초과 시 경고 | 분산된 것처럼 보이는 쏠림 |
| 롤링 12개월 수익률 표시 | 좋은 기간만 보여 주기 |
| 섹터 요약에서 종목 2개 이하 섹터를 최고·최저 판정에서 제외 | 한 종목을 섹터 이야기로 부풀리기 |
| 연구 노트에 모든 실행을 자동 기록 + 최고기록 곡선 | 시도 횟수 숨기기 |
| 알파 저장 시 일별 손익을 남겨 다음 알파와 자기상관 비교 | 이름만 바꾼 같은 알파 |
| 채점 구간 확인 횟수를 세어 표시하고 기록 | 성적 나올 때까지 채점 구간 반복 조회 |
| 시뮬레이션 기간을 반으로 갈라 앞뒤 Sharpe를 함께 표시 | 특정 시기에만 통한 알파 |
| 알파 평가도 채점 구간을 제외하고 계산 | 채점 구간 엿보기 |


## IQC(WorldQuant) 방식 그대로

**알파 만들기** 화면은 WorldQuant BRAIN을 축소해 옮긴 것입니다.

```
rank(mom12_1) - 0.6 * rank(vol120)
group_neutralize(rank(mom12_1), sector)
-ts_corr(rank(close), rank(volume), 20)
```

식 언어는 BRAIN Fast Expression의 부분집합입니다 — 이름을 일부러 같게 맞췄습니다.
횡단면(`rank` `zscore` `scale` `winsorize` `quantile` `group_neutralize` `group_rank`),
시계열(`ts_mean` `ts_std_dev` `ts_sum` `ts_max` `ts_min` `ts_rank` `ts_delta` `ts_delay`
`ts_corr` `decay_linear`), 산술(`abs` `log` `sqrt` `sign` `power` `min` `max` `if_else`).
데이터 필드는 `close` `returns` `volume` + 팩터 9개.

식을 쓰면 BRAIN과 같은 순서로 롱숏 북이 만들어집니다.

```
알파 식 → 중립화(시장/섹터) → 종목 상한 → 비중 정규화(|합|=1) → 감쇠(decay) → 롱숏 북
```

채점 기준도 BRAIN 제출 기준을 그대로 씁니다.

| 항목 | 기준 | 뜻 |
|---|---|---|
| Sharpe | ≥ 1.25 | 수익 / 변동성 (연율) |
| Fitness | ≥ 1.00 | Sharpe × √(\|수익률\| / max(회전율, 12.5%)) |
| Turnover | 1 ~ 70% | 하루에 북의 몇 %를 갈아치우나 |
| 자기상관 | < 0.70 | 내가 이미 낸 알파와 손익이 얼마나 닮았나 |

Fitness의 분모에 회전율이 있다는 것이 핵심입니다. **많이 사고팔아 낸 성과는 깎입니다.**
그래서 학생이 배우는 것은 "더 잘 맞히는 법"만이 아니라 "덜 갈아타는 법"입니다.

알파를 저장하면 일별 손익이 함께 남고, **알파 묶음** 패널이 저장한 알파들을 **같은 위험으로**
합쳐 보여 줍니다(같은 금액으로 섞으면 변동성 큰 알파가 묶음을 지배해 버립니다).
개별 평균보다 묶음 Sharpe가 높으면 분산이 실제로 작동한 것입니다.
좋아지지 않았을 때는 원인을 구분해서 말합니다 — 알파끼리 닮아서인지, Sharpe가 음수인
약한 알파를 섞어 희석된 것인지, 표본이 짧아서인지는 처방이 다르기 때문입니다.

**링크 복사**를 누르면 식과 시뮬레이션 설정이 주소에 실립니다
(`#alpha!<식>!<중립화>-<감쇠>-<상한>-<기간>`). 서버 없이 수업에서 같은 출발선을 만들 수 있습니다.

알파가 기준을 넘겼다면 **채점 구간에서 확인**으로 잠겨 있던 최근 12개월에 같은 알파를 돌려
IS와 OS를 나란히 봅니다. 개발 구간에서 좋았던 알파가 무너지는 것을 직접 보는 것이 이 사이트의
핵심 경험입니다. 다만 여러 번 보면 그 구간도 개발 구간이 되므로 **확인 횟수를 세어 보여 주고**
연구 노트에 남깁니다 — 막지 않되 정직하게 기록하는 쪽을 택했습니다.

**두 화면의 숫자가 다른 이유도 적어 두었습니다.** 알파 만들기는 IQC 방식이라 롱숏이고,
전략 실험실은 롱온리입니다. 같은 알파라도 롱온리는 시장이 오르면 같이 오르고 롱숏은 그 부분이
빠지므로 샤프도 회전율도 다르게 나옵니다. 어느 쪽이 맞는 게 아니라 보는 렌즈가 다릅니다.

**다른 점도 명시했습니다.** BRAIN은 전 세계 수천 종목과 재무·뉴스 데이터를 씁니다.
여기는 나스닥100 약 100종목과 가격·거래량뿐입니다. 연산자와 채점 방식은 같지만
데이터의 폭이 다르므로, 여기 결과가 거기서 재현된다는 보장은 없습니다.
배우기 → IQC 로드맵에 이 한계를 적어 두었습니다.


## 데이터

| 항목 | 내용 |
|---|---|
| 종목 | 나스닥100 구성 종목 + QQQ·나스닥종합·S&P500 |
| 기간 | 약 12년 일봉 (수정주가) |
| 분봉 | 대표 16종목 30분봉, 최근 60일 |
| 매크로 | 미국 2년·10년물 금리, 장단기 금리차, VIX, 하이일드 스프레드, 연방기금금리 |
| 출처 | yfinance(주가) + FRED 공개 CSV(매크로) — **API 키 불필요** |
| 갱신 | GitHub Actions가 평일 매일 자동 수집·커밋 (UTC 22:30 = 한국시간 07:30) |

**가상 데이터는 저장소에 두지 않습니다.** 화면 개발용 합성 데이터는 `docs/data-dev/`로 분리되어
있고 배포에 포함되지 않습니다(`?dev=1`로만 접근).

### 수동으로 데이터 갱신하기

```bash
pip install numpy pandas yfinance
python -m pipeline.fetch_market            # 전체
python -m pipeline.fetch_market --skip-intraday   # 일봉만
```

또는 GitHub → Actions → "시장 데이터 자동 갱신" → Run workflow.


## 순위표 (Supabase)

서버 없이 브라우저에서 Supabase REST API를 직접 부릅니다.
`docs/js/config.js`의 publishable 키는 공개를 전제로 만들어진 키이며,
실제 보호는 데이터베이스의 RLS 정책이 합니다.

- 읽기: 누구나
- 쓰기: 닉네임 2~20자, 최소 60거래일, 수익률 범위, 거래기록 필수 조건을 통과할 때만
- 수정·삭제: 아무도 못 함

설정: Supabase SQL Editor에서 `supabase/schema.sql` 실행.


## 배포

GitHub Pages (`/docs` 폴더). 정적 파일뿐이라 서버가 필요 없습니다.

```
docs/
  index.html
  css/terminal.css
  js/
    core/       util·charts·metrics·ml   (계산 엔진)
    data.js     시장 데이터 로더
    curriculum.js  14단계 학습 경로
    journal.js     연구 노트 (localStorage)
    alpha.js       사용자 정의 알파 (localStorage)
    expr.js        알파 식 언어 (BRAIN Fast Expression 축소판)
    sim.js         롱숏 북 시뮬레이터 + IQC 채점
    config.js   Supabase 설정
    leaderboard.js
    strategies.js      고전 퀀트 기법 11종
    ai_strategies.js   AI 모델 4종 (워크포워드 학습)
    app.js      화면 뼈대
    screens/    home·market·intraday·factor·alpha·strategy·trade·rank·journal·learn
  data/         GitHub Actions가 매일 갱신 (실데이터)
```


## 디자인

트레이딩 터미널 방향입니다. 액센트는 **앰버(호박색)** — 초창기 금융 단말기가 앰버 인광 CRT를 쓴 데서
왔습니다. 숫자는 전부 고정폭이라 자리수가 맞고, 상승/하락 색은 액센트와 분리된 의미 색이라
다른 용도로 쓰지 않습니다. 가격 같은 '값'에는 색을 칠하지 않고 등락에만 씁니다.

어두운 테마 하나로 갑니다(의도된 선택).


## 관련 문서

- [README.md](README.md) — 프로젝트 전체 소개
- [QUANT.md](QUANT.md) — GPU 학습 퀀트 파이프라인 (`quant/`)
- [COMPETITIONS.md](COMPETITIONS.md) — 나가 볼 만한 대회 조사


교육·연구용입니다. 실제 투자 판단에 사용하지 마십시오.
