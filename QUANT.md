# 퀀트 AI 파이프라인 (`quant/`)

GPU로 학습하는 본격적인 퀀트 버전입니다. 앞의 `phase1a~phase3`나 웹 화면이
"지수 하나가 오를까 내릴까"를 다뤘다면, 여기서는 실제 퀀트 운용이 하는 방식으로 바꿉니다.

| | 기존 (phase / 웹 1~7탭) | 퀀트 (`quant/`, 웹 8탭) |
|---|---|---|
| 대상 | 지수 1개 | 종목 수십~수백 개 |
| 문제 | 방향 맞히기 (오른다/내린다) | 상대 순위 맞히기 (누가 더 오를까) |
| 목표 | 정확도·AUC | IC(예측과 실제 수익률의 상관) |
| 학습 | 브라우저 JS / scikit-learn | PyTorch (GPU), IC 손실 직접 최적화 |
| 검증 | 시간순 분할 / 워크포워드 | 워크포워드 + purge + embargo |
| 운용 | 사거나 현금 | 상위 K종목 롱(선택적 롱숏), 리밸런싱·회전율·비용 |


## 빠른 시작

```bash
pip install -r requirements.txt

# 1) 인터넷 없이 파이프라인 점검 (가상 데이터, 1~2분)
python -m quant.run --preset demo

# 2) 실제 미국 대형주 + 미국 매크로, GRU, GPU 학습
python -m quant.run --preset us --model gru --device cuda

# 3) 한국 대형주
python -m quant.run --preset kr --model gru --device cuda
```

결과는 `runs/<이름>/` 에 저장됩니다.

| 파일 | 내용 |
|---|---|
| `config.json` | 이 실험에 쓴 설정 전부 (재현용) |
| `report.json` | IC·분위수익·성과 지표 |
| `equity.png` `ic.png` `quantiles.png` | 그림 |
| `web_export.json` | 웹의 **8. 퀀트 AI** 탭에 올리면 결과를 보고 모의투자까지 가능 |


## 무엇을 하는가

### 1단계 — 데이터 (`data.py`)
`yfinance`로 종목들을 한 번에 받고, FRED 공개 CSV(키 불필요)로 금리·스프레드·VIX를 붙입니다.
받은 데이터는 `data_cache/`에 저장되어 다음 실행부터는 즉시 시작합니다.
인터넷이 없으면 `--source synthetic`으로 가상 시장을 만들어 씁니다.

### 2단계 — 팩터 (`features.py`)
모멘텀(5·20·60·120일, 12-1개월), 이격도, 변동성, 하락변동성, 시장베타, 거래량 급증,
**금리 민감도**(최근 120일 동안 금리가 움직일 때 이 종목이 같이 움직인 정도), 매크로 z점수 등
20여 개를 만들고, **날짜마다 종목들 사이의 순위(-1~+1)로 바꿉니다**(횡단면 정규화).

이렇게 하면 시장 전체가 폭락한 날에도 값의 크기가 변하지 않아 학습이 안정적이고,
"시장이 올랐다"가 아니라 "남들보다 잘했다"만 남습니다.

### 3단계 — 라벨
`horizon`일 뒤 수익률에서 그날 전체 평균을 뺀 **초과수익**을 다시 횡단면 순위로 바꿔 씁니다.
시장 방향을 맞히는 문제가 아니라 종목을 줄 세우는 문제로 만드는 것입니다.

### 4단계 — 모델 (`models.py`, `losses.py`)
```
mlp          오늘 하루치 팩터 → 점수        (빠르고 튼튼한 기본)
gru          최근 N일 흐름 → 점수           (LSTM 계열)
transformer  최근 N일 중 중요한 날에 집중    (어텐션)
ridge        선형 비교군 — 이걸 못 이기면 신경망은 의미 없음
lgbm         LightGBM 비교군
```
손실 함수가 핵심입니다. 기본값 `--loss ic`는 오차(MSE)가 아니라
**같은 날 종목들 사이의 상관(IC)을 직접 최대화**합니다. 값이 틀려도 순서만 맞으면 되기 때문입니다.

### 5단계 — 검증 (`datasets.py`, `train.py`)
```
[--------- 학습 ---------][purge][검증][embargo][--- 시험(예측) ---]
```
- **purge**: 라벨이 `horizon`일 뒤를 보므로 학습 끝부분의 정답이 검증 구간과 겹칩니다. 그만큼 잘라냅니다.
- **embargo**: 겹치지 않아도 바로 옆 날짜는 너무 닮아 있어 며칠 더 비웁니다.
- 시험 구간이 끝나면 그 구간을 학습에 넣고 다시 배웁니다(워크포워드).
- 조기 종료 기준은 손실이 아니라 **검증 IC**입니다.
- 시드를 바꿔 여러 번 학습해 평균냅니다(`--ensemble`).

### 6단계 — 포트폴리오 (`backtest.py`)
`rebalance`일마다 점수 상위 `top_k` 종목을 동일가중으로 담고, 갈아탈 때 바뀐 비중만큼
거래비용과 슬리피지를 뺍니다. 비교 대상은 **같은 종목들의 동일가중 포트폴리오**입니다.
이걸 못 이기면 종목을 고른 의미가 없습니다.


## 결과 읽는 법

```
IC 평균 0.02 ~ 0.05   실무에서 쓸 만한 신호 (방향 정확도로는 51~53% 수준)
IC 평균 0.05 ~ 0.10   상당히 좋은 신호
IC 평균 0.30 이상     정상적인 시장 데이터에서는 나오지 않습니다. 누수를 의심하세요.
IC t값 |t| > 2        우연으로 보기 어렵다
```

성과보다 **IC → 분위별 수익 → 비용 후 성과** 순으로 확인하세요.
비용 전에는 좋은데 비용 후에 사라진다면, 신호는 있지만 너무 자주 갈아탄다는 뜻입니다.
`--rebalance`를 늘리거나 `--horizon`을 늘려 회전율을 낮춰 보세요.


## 누수를 스스로 점검하는 두 가지 방법

이 프로젝트에서 가장 중요한 습관입니다. **좋은 결과가 나오면 먼저 의심하세요.**

```bash
# 1) 예측할 거리가 전혀 없는 시장 → IC가 0 근처여야 정상
python -m quant.run --preset demo --syn-signal 0 --name check_nosignal

# 2) 정답을 날짜별로 뒤섞기 → 특징과 정답의 관계가 끊기므로 IC가 0 근처여야 정상
python -m quant.run --preset demo --permute-labels --name check_permute
```

실제로 이 저장소를 만들 때 두 검사가 문제를 잡아냈습니다.
처음 만든 가상 시장은 섹터 요인을 `0.85`의 지속성으로 이어지게 만들어서,
`--syn-signal 0`으로 두어도 '섹터 모멘텀'이라는 예측 가능한 구조가 남아 IC가 0.22까지 나왔습니다.
코드 누수는 아니었지만 "신호 0 = 예측 불가능"이라는 점검의 전제가 깨져 있었으므로 생성기를 고쳤습니다.
지금은 두 검사 모두 IC가 0 근처(0.00, t<2)로 나옵니다.

측정해 둔 기준값(가상 데이터, 40종목):

| 숨은 신호 | 측정된 IC | 의미 |
|---|---|---|
| 0.00 | -0.000 (t=-0.45) | 예측 불가능. 파이프라인이 깨끗하다는 증거 |
| 0.10 | +0.042 (t=4.34) | 실제 퀀트에서 '쓸 만하다'고 보는 수준 |
| 0.15 | +0.100 (t=6.81) | 아주 좋은 신호 |
| 0.30 | +0.223 (t=16.8) | 현실에 없는 수준. 모델이 배우는지 확인할 때만 |


## GPU (NVIDIA A6000) 활용

A6000(48GB, Ampere)이면 이 파이프라인은 여유롭습니다.

```bash
# CUDA 빌드 설치 (드라이버 버전에 맞춰 cu121/cu124 등 선택)
pip install torch --index-url https://download.pytorch.org/whl/cu124

python -c "import torch; print(torch.cuda.is_available(), torch.cuda.get_device_name(0))"
```

```bash
# 넉넉하게 키운 설정 (A6000 기준)
python -m quant.run --preset us --model transformer --device cuda \
    --hidden 256 --layers 4 --seq-len 60 --batch-dates 64 \
    --epochs 60 --ensemble 5 --top-k 15 --rebalance 10
```

- `--batch-dates`가 실질적인 배치 크기입니다. 날짜 하나에 종목 수만큼 표본이 들어가므로
  `배치 표본 수 ≈ batch_dates × 종목 수`입니다. 64 × 500종목 = 32,000 표본이라도 A6000에서는 가볍습니다.
- bf16 혼합정밀도가 기본으로 켜집니다(`--no-amp`로 끌 수 있음). Ampere 이상에서 유효합니다.
- 앙상블을 늘리는 것이 모델을 키우는 것보다 대개 효과적입니다. 금융 데이터는 잡음이 커서
  큰 모델이 쉽게 과적합합니다. `--ensemble 5~10`을 먼저 시도하세요.
- GPU가 없어도 `--device cpu`로 그대로 돌아갑니다(데모 프리셋 기준 30~60초).


## 자주 쓰는 실험

```bash
# 예측 시계를 늘려 회전율 낮추기
python -m quant.run --preset us --horizon 20 --rebalance 20 --name us_h20

# 롱숏 (하위 종목 공매도까지)
python -m quant.run --preset us --long-short --top-k 15 --name us_ls

# 거래비용을 올리면 언제 우위가 사라지는지
python -m quant.run --preset us --cost 0.002 --slippage 0.002 --name us_cost

# 모델 비교 (같은 데이터·같은 구간)
for m in ridge lgbm mlp gru transformer; do
  python -m quant.run --preset us --model $m --name us_$m
done
```


## 알아 둘 한계

- **생존 편향**: `config.py`의 종목 목록은 지금 살아남은 종목들입니다. 상장폐지된 종목이 빠져 있어
  성적이 실제보다 좋게 나옵니다. 보고서에는 반드시 이 점을 밝혀야 합니다.
- **거래 가능성**: 종가에 원하는 만큼 체결된다고 가정합니다. 실제로는 유동성·호가 단위·공매도
  가능 여부에 걸립니다. 특히 롱숏은 대차 비용을 전혀 반영하지 않았습니다.
- **다중검정**: 설정을 여러 번 바꿔 돌리면 그중 하나는 우연히 좋아 보입니다. 시도 횟수를 적어 두고,
  좋은 결과는 다른 기간·다른 시장에서 재현되는지 확인하세요.
- **가상 데이터**: `--preset demo`의 결과는 실제 시장 성과가 아닙니다. 화면과 리포트에 항상 표시됩니다.

교육·연구용입니다. 실제 투자 판단에 사용하지 마십시오.
