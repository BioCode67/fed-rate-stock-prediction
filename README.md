# 금리 정책 기대와 주가 변동 예측 연구

영남대학교 SW중심대학사업단 「AI·SW 러닝메이트 멘토링 프로그램」
**주제: 중앙은행의 금리 정책 기대가 주가 변동에 미치는 영향**

---

## 이 연구가 진짜로 하려는 것

우리가 만드는 것은 헤지펀드가 쓰는 것과 **같은 도구**입니다.
다만 목표는 **돈을 버는 것이 아니라, 시장이 왜 이토록 예측하기 어려운지를 데이터로 증명하는 것**입니다.

그래서 이 프로젝트에서는:

- 정확도 숫자를 억지로 높이지 않습니다.
- **낮더라도 정직한 정확도가, 부풀린 높은 정확도보다 훨씬 값집니다.**
- 정확도가 90%처럼 비현실적으로 높으면 → 십중팔구 **데이터 누수(미래 정보가 새어든 버그)** 입니다. 기뻐하지 말고 버그를 찾으세요.
- 결과가 "예측이 어렵다"로 나와도 그것은 **효율적 시장 가설(EMH)** 과 일치하는 의미 있는 결론입니다. 실패가 아닙니다.

---

## 전체 계획 (한눈에)

| Phase | 파일 | 내용 | 상태 |
|---|---|---|---|
| 1-a | `phase1a_model_comparison.py` | 4모델 비교 + 평가지표 확장 + 시계별/금리변수/중요도 실험 | ✅ 작성 완료 |
| 1-b | `phase1b_backtest.py` | 워크포워드 + 거래비용 백테스트 | ⏳ 예정 |
| 1-c | `phase1c_garch_volatility.py` | GARCH 변동성 분석 | ⏳ 예정 |
| 2 | `phase2_event_study.py` | 연준 성명서 이벤트 스터디 (NLP) | ⏳ 예정 |
| 3 | `phase3_cross_section.py` | 개별 종목 순위 예측 포트폴리오 | ⏳ 예정 |

> Phase 1만 제대로 끝내도 완결된 발표가 됩니다. 2·3은 여유가 있을 때 붙이는 보너스입니다.

---

## Phase 1-a 실행법 (Google Colab)

1. [Google Colab](https://colab.research.google.com) 에 접속해 새 노트를 만듭니다.
2. `phase1a_model_comparison.py` 의 **전체 내용**을 코드 셀에 붙여넣습니다.
3. 맨 위 설치 줄의 `#` 을 지우고 한 번 실행합니다:
   ```python
   !pip install fredapi scikit-learn statsmodels xgboost tensorflow scipy matplotlib koreanize-matplotlib
   ```
4. `[1] 설정` 구역에서 **FRED API 키**를 채웁니다:
   ```python
   FRED_API_KEY = "여기에_발급받은_키"
   ```
   (FRED 키 무료 발급: https://fred.stlouisfed.org/docs/api/api_key.html )
5. 셀을 실행하면 표와 그래프가 차례로 나옵니다.

### 멘티들이 바꿔가며 실험할 값 (`[1] 설정`)

| 상수 | 의미 | 기본값 |
|---|---|---|
| `PRIMARY_HORIZON` | 며칠 뒤 방향을 맞힐지 (대표값) | `5` |
| `HORIZONS` | 시계 민감도 실험에 쓸 값들 | `[1, 5, 20, 60]` |
| `SEQ_LEN` | LSTM이 되돌아보는 과거 길이 | `20` |
| `EPOCHS` | LSTM 학습 반복 횟수 | `20` |
| `TRAIN_RATIO` | 학습:시험 분할 비율 | `0.8` |

---

## 결과 읽는 법 (가장 중요)

### 1. 정확도만 보지 마세요 — **기준선(baseline)** 과 비교하세요
"항상 상승에 베팅"만 해도 나오는 정확도가 **기준선**입니다.
상승일이 55%면 아무 생각 없이 찍어도 55%가 나옵니다.
표의 **`정확도-기준선`** 이 **양수**여야 모델이 의미 있는 것입니다.

### 2. `p값(vs50%)` 이 0.05보다 크면
모델의 성적이 **동전 던지기와 통계적으로 구별되지 않는다**는 뜻입니다.
즉 "우연일 수 있다". 이게 나쁜 게 아니라, 시장이 효율적이라는 정직한 증거입니다.

### 3. 예측 시계가 길수록 정확도가 올라 보이면
기준선도 함께 오른 것일 수 있습니다(장기적으로 주가는 상승 경향).
그래서 표에 **괄호로 `기준선 대비`** 를 함께 적어 뒀습니다. 그 값으로 판단하세요.

### 4. 변수 중요도에서 금리가 높게 나와도
실험 B(금리변수 유무)에서 정확도가 오르지 않았다면, 그 중요도는
**"지금이 몇 년도인지 알려주는 표식(시기 프록시)"** 로 작동한 것일 수 있습니다.
중요도가 높다고 곧 예측력이 있는 건 아닙니다.

---

## 우리가 검증하는 가설 (결론이 아니라 '검증 대상')

| # | 가설 | 이 가설이 **틀렸다**고 볼 조건 |
|---|---|---|
| H1 | 모델 계열 간 정확도·속도·해석가능성 트레이드오프가 있다 | 한 모델이 세 축 모두 우월 |
| H2 | 금리변수를 넣어도 정확도가 크게 안 오른다 (효율적 시장) | 금리변수로 정확도가 유의하게 상승 |
| H3 | 금리 '수준'의 높은 중요도는 시기 프록시 효과다 | 시기를 통제해도 중요도 유지 |

> **가설과 반대 결과가 나오면 그대로 보고합니다.** 가설을 살리려 실험 설정을 바꾸지 않습니다.
> H2는 지지되든 반박되든 둘 다 의미 있습니다. (반박되면 더 흥미로운 발견!)

---

## 방법론 원칙 (코드가 지키고 있는 것)

1. **정상성 확보** — 주가·금리의 절대 수준 대신 비율(`주가/이동평균-1`)·변화량(`금리.diff()`)을 씁니다.
2. **기준선 함께 보고** — 다수결 정확도를 항상 같이 출력합니다.
3. **시간순 분할** — 데이터를 절대 무작위로 섞지 않습니다(미래 정보 차단).
4. **데이터 누수 방지** — 스케일러는 학습 구간에만 `fit` 합니다.
5. **재현성** — 모든 난수 시드를 42로 고정합니다.
6. **표본 수 확보** — 모든 거래일(약 2,200개)을 씁니다.

---

## ⚠️ 반드시 기억할 것

- 이 코드는 **교육·연구용**입니다. **실제 투자 판단에 쓰지 마세요.**
- 백테스트가 좋아도 **미래 수익을 보장하지 않습니다.**
- 데이터를 **지어내지 않습니다.** 실제로 못 구하는 데이터는 분석하지 않습니다.

---

## 참고문헌 (자료조사용)

- Bernanke & Kuttner (2005), *Journal of Finance* — 예상 밖 25bp 인하 → 주가 약 1%↑. 핵심은 금리 수준이 아니라 **서프라이즈**. https://www.nber.org/papers/w10402
- Gu, Kelly & Xiu (2020), *RFS* — ML 모델을 자산가격 예측에 비교. https://www.nber.org/papers/w25398
- Welch & Goyal (2008), *RFS* — 예측 변수 대부분이 실제로는 예측력이 나빴다. https://www.nber.org/papers/w10483
- Shah, Paturi & Chava (2023), *ACL* — FOMC 매파/비둘기 데이터셋(Phase 2용). https://arxiv.org/abs/2305.07972
